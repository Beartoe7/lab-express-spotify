<div>
    {{#each tracks}}
        <div>
            Name: {{this.name}}
            <audio controls>
                <source src={{this.preview_url}}>
            </audio>
        </div>
    {{/each}}
</div>